[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "dependency-manager"
version = "1.0.0"
description = "Self-healing dependency manager for Python scripts"
requires-python = ">=3.8"

[tool.setuptools]
py-modules = ["dependency_manager"]
